extends ../../layouts/default
include ../../mixins/box-head
include ../../mixins/product-layout

block main 
    .container.my-3 
        .row 
            .col-12 
                +box-head("Kết quả tìm kiếm")
            
        if(products.length > 0)
            +product-grid(products)
        else
            h3 Không tìm thấy sản phẩm nào